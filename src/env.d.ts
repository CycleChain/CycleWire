/** Replaced at build time: `true` in the development build, `false` in production. */
declare const __DEV__: boolean;

/** Replaced at build time with the package version. */
declare const __VERSION__: string;
