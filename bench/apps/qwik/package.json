{
  "name": "bench-qwik",
  "description": "The Wirestore page built with Qwik City and its Node.js server adapter",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "qwik build",
    "build.client": "vite build",
    "build.server": "qwik check-client src dist && vite build -c adapters/node-server/vite.config.ts",
    "build.types": "tsc --incremental --noEmit",
    "dev": "vite --mode ssr",
    "serve": "node server/entry.node-server",
    "qwik": "qwik"
  },
  "devDependencies": {
    "@builder.io/qwik": "1.20.1",
    "@builder.io/qwik-city": "1.20.1",
    "@types/node": "26.6.2",
    "ignore": "7.0.10",
    "typescript": "7.0.2",
    "undici": "8.11.0",
    "vite": "8.3.0",
    "vite-tsconfig-paths": "6.1.1"
  },
  "//": "The qwik CLI (qwik build) loads 'ignore' but @builder.io/qwik 1.20.1 does not declare it; the starter only gets it through ESLint, so it is listed here at the version Qwik pins.",
  "engines": {
    "node": ">=22.12"
  }
}
