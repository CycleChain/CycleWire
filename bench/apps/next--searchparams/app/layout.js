// The next app's root layout: the header with the cart, read from the cart
// cookie on every request, the page, the @modal slot and the footer.
export { default, metadata } from '@apps/next/app/layout.js';
