// The next app's quick view on a full page load of /products/<id>: the
// dialog, open, as part of the page, which is how it works without
// JavaScript. app/products/[id]/page.js renders the shop behind it.
export { default } from '@apps/next/app/@modal/products/[id]/page.js';
